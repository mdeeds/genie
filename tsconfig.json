{
  "compilerOptions": {
    "target": "es6",
    "module": "commonjs",
    "sourceMap": true,
    // TFJS doesn't compile in Typescript, so we need to skip the checks.
    // https://www.tensorflow.org/js/tutorials/setup
    "skipLibCheck": true,
    "sourceRoot": "ts",
    "outDir": "src",
  }
}